# The range will always exclude the upper limit. 
# Printing the numbers from 50 to 71.
for i in range(50,71,2):
    print(i)

# Range also provides a third argument which is passed as a skip element number.
# For example, here, the elements will be incremented in the intervals of 2.